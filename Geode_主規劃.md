# Geode ── 個人視覺 × 文字收藏中樞|主規劃

> 專案暫定代號 **Geode**（晶洞）。整份規劃刻意把「名字」當成一個變數處理，
> 你選定後全域替換即可，架構不受影響。候選：Geode / Cairn / Loom / Trove。

---

## 一、這個東西到底是什麼

一個吃下多平台分享的個人收藏庫，把圖片與文字都收進同一個地方，
自動打標籤、萃取色票，最後餵養你的飾品設計工作流，並成為第二大腦生態系裡
可被調用的素材倉庫。未來可對外發布。

它不是「又一個 App」，而是把 Keepbox 已經在做的事（社群貼文、HTML/MD、標籤分類）
往前推一步：多一個穩定的收件入口，多一層 AI 智慧，多一條輸出到設計與 Notion 的路。

核心使用者是你自己。先做到你每天都在用，商業化才有意義。

---

## 二、四層架構

整個系統只有四層，先把這張圖記在心裡，後面每個 Phase 都是在填其中一層。

| 層 | 做什麼 | 對應 Phase |
|---|---|---|
| **入口層 Capture** | 接住各平台分享 / 網頁 | P2（圖）、P5（文字）、日後 Chrome 擴充 |
| **核心層 Store** | 標準化格式、把圖存進自有空間 | P1 |
| **智慧層 Enrich** | AI 標籤、色彩萃取、prompt 反推 | P3、P4、P7 |
| **輸出層 Surface** | App 瀏覽、Notion、export、餵 agents | P6、P8 |

彈性的關鍵在「核心層」。只要進到資料庫的東西都是同一種格式，
上面三層要換、要加、要拆給別人用，都不會動到地基。

---

## 三、資料標準化：一句話講懂

不同平台分享出來的東西格式天差地遠——小紅書給你一個 App 連結、抖音給你影片、
Pinterest 給你圖片直連。**標準化** 就是不管入口是誰，落地時都轉成同一張表：

| 欄位 | 說明 |
|---|---|
| `id` | 唯一識別 |
| `content_type` | `image` / `text`，決定走哪條處理路徑 |
| `media_path` | 圖片／影片封面 **下載後存在自己空間** 的路徑（不存平台 CDN 連結，會過期） |
| `source_platform` | xiaohongshu / instagram / douyin / pinterest / threads / facebook / web |
| `source_url` | 原始貼文連結 |
| `author` | 原作者（常抓不到，允許留空；商業化時的 attribution 依據） |
| `tags` | AI + 手動標籤陣列 |
| `palette` | 主色 + 輔色 HEX（圖片才有） |
| `text_body` | 抽取後的正文（文字型才有） |
| `folder` | 所屬檔案夾 |
| `saved_at` | 收藏時間戳 |
| `status` | pending / processed（配合背景佇列） |

寫一段「轉換器」把各平台的分享塞進這張表，後面所有智慧與輸出都只認這張表。

---

## 四、標籤維度設計

不要做成一棵大分類樹，會卡死。拆成互相獨立的維度：

- **品項類型**（單選）：手鏈 / 項鍊 / 戒指 / 耳環 / 其他
- **材質元素**（可複選）：水晶名稱（拿你現有的水晶知識庫當種子標籤庫）、串珠、金屬線
- **色彩**（AI 自動，不用手想）：跑色彩萃取自動產出
- **來源創作者**（獨立欄位，非標籤）
- **編織風格**（**先不要硬分類**）：讓 AI 先打自由文字標籤（螺旋纏繞 / 網狀 / 幾何…），
  存個 20–30 張後回頭看標籤聚類，分類法自己會長出來。先蒐集，再定義。

---

## 五、九個 Phase（40 步 + 9 道驗收關卡）

每個 Phase 標了目標與驗收。步驟清單與可勾選版本在 HTML 驗收檔。

### P0 命名與品牌（地基前的地基）
選定名字、查 App Store 名稱可用性、更新專案內部識別、（選配）佔位帳號、
Logo 方向可延用你既有的 𓆸 / 𖤓 符號系統。
**驗收：** 新名字定案，App Store 無強碰撞，專案內識別已換。

### P1 資料骨架（schema + storage）
定義上面那張標準化表、選定 storage（本地 SQLite 起步，或 Supabase / R2）、
圖片下載與存放邏輯、content_type 分流、App Group 共享容器、待處理佇列。
**驗收：** 一筆假資料能完整走完「進佇列 → 下載 → 落地成標準格式」。

### P2 iOS Share Extension（圖片平台）
建 Extension target、Info.plist 宣告 activation rule、ShareViewController 接
NSExtensionItem、**只做輕量落地**（寫進 App Group 佇列後立刻結束，Extension 記憶體吃緊）、
主 App 讀佇列做重工、逐平台實測（小紅書 → IG → 抖音封面 → Pinterest）。
**驗收：** 四個圖片平台分享皆能落地成標準 item；抖音抓得到影片封面。

### P3 AI 自動標籤
定義標籤維度、接 vision model（Claude API）產生標籤、用水晶知識庫初始化標籤庫、
編織風格走自由文字標籤先不硬分類。
**驗收：** 圖片進來自動產出可用的品項 / 材質 / 風格標籤。

### P4 色彩萃取
主色 + 輔色萃取（colorthief 或簡單 k-means）、輸出 HEX 存進 palette 欄位。
**驗收：** 每張圖有可一鍵複製的色票。

### P5 文字收藏路徑（FB / Threads / 網站）
文字型分享接收、網頁正文抽取（Readability 演算法，去廣告雜訊）存成 md/html、
銜接 Keepbox 既有閱讀器。圖與文兩條路徑並行，用 content_type 分流。
**驗收：** 好文 / 梗文分享能存成可讀清單，不與圖片路徑打架。

### P6 Notion sync
Notion database schema 對應、metadata + 縮圖 URL 同步（**圖本體留自有 storage**，
Notion 只當檢索介面）、決定單向或雙向同步。
**驗收：** 收藏出現在 Notion 且可用標籤 / 平台檢索。

### P7 設計工作流串接（真正的殺手鐧）
選圖 → 反推可丟 Midjourney/Flux 的 prompt、一組收藏 → AI 產出一份設計 brief
（共同配色 / 線條 / 風格語言）、mood board 排圖輸出給客戶初稿。
**驗收：** 從收藏庫能一鍵產出設計初稿素材包。

### P8 第二大腦 / 對外化
Export / query 層（讓你的 Slack @hohoho 多代理系統能查詢這個庫）、
多使用者資料隔離、定價與上架準備。
**驗收：** 有可對外發布的最小版本，且資料能被 agents 調用。

---

## 六、我幫你補的擴充（依價值排序）

1. **一批圖 → 一份設計 brief**：選一組收藏，AI 總結共同的設計語言，直接變方向書。
   比單張反推 prompt 更貼近「給別人做初稿」的真實需求。（已納入 P7）
2. **視覺相似搜尋**：用 embedding 在自己庫裡找風格接近的圖，設計找參考省時。
3. **餵給 Slack 多代理系統**：只要 schema 乾淨、有 export 層，這庫就是 agents 的素材源。
   這是「第二大腦倉庫」那句話的技術落點。（已納入 P8）
4. **版權與 attribution**：對外商品化時提醒使用者尊重原作者，author 欄位已預留。
5. **重複偵測**：同張圖從不同平台存兩次自動去重。
6. **連結健康度**：定期標記失效原連結；因為你存了圖本體，圖不會死，只是溯源會斷。
7. **離線優先**：捷運上滑到先進佇列，沒網路也能收，有網再處理。
8. **匯出成 Mué Studio 素材包**：直接生可貼 Threads / 賣貨便的圖文草稿，接你現有的自動上架流。
9. **skill 化**：把「收藏 → 設計初稿」整條線包成 Claude skill，未來別人也能用你的方法。

---

## 七、模型使用建議

- 日常寫 Swift、除錯、Share Extension → Claude Code + Sonnet 5（預設，快又準）
- 一次性的高槓桿架構決策（App Group 資料流、標準化 schema）→ Opus 4.8 討論定案，回 Sonnet 實作
- Fable 5 留給投資分析那種長推理鏈場景，這個專案用不到

---

## 八、彈性設計三原則（為什麼這樣規劃）

1. **名字是變數**：不把名字寫死在架構、bundle 命名策略上，換名不動地基。
2. **schema 先行**：所有智慧與輸出都只認標準化表，換平台、加功能、拆給別人用都不傷筋骨。
3. **入口與處理解耦**：Extension 只負責「接住並落地」，下載 / AI / 排版都在主 App。
   之後加 Chrome 擴充、加平台，都是多一個入口，核心不變。

先讓它跑通「小紅書找靈感 → 存圖 → 出設計初稿」這條線，你自己天天用，再談對外賣。
